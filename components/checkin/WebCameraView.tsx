// Native stub — never rendered on iOS/Android (QRScannerModal uses CameraView directly on native)
export function WebCameraView(_props: { facing: 'front' | 'back'; onQRDetected: (data: string) => void }) {
  return null;
}
